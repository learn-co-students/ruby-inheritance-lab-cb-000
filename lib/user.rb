class User
  attr_accessor :first_name, :last_name
  # def initialize(first_name, last_name)
  #   @first_name = first_name
  #   @last_name = last_name
  # end
  # Users probably aren't initialized, as they are generic.
  # Only actual teachers and students get initialized.
end
